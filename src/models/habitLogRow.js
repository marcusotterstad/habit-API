class HabitLogRow {
    constructor(id, date, habitId, userId, duration, notes) {
        this.id = id;
        this.date = date;
        this.habitId = habitId;
        this.userId = userId;
        this.duration = duration;
        this.notes = notes
    }
}